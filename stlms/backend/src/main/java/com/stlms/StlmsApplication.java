package com.stlms;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the Smart Traffic Light Management System (STLMS).
 * Demonstrates the State Design Pattern through a real-world traffic control scenario.
 */
@SpringBootApplication
public class StlmsApplication {

    public static void main(String[] args) {
        SpringApplication.run(StlmsApplication.class, args);
    }
}
